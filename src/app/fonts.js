import { Inter, Outfit } from 'next/font/google';

export const inter = Inter({
    subsets: ['latin'],
    display: 'swap',
})
   
export const outfit = Outfit({
  subsets: ['latin'],
  display: 'swap',
})